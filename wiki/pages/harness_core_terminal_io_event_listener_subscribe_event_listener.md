---
name: "harness_core.terminal_io.event_listener.subscribe_event_listener"
description: "Create and subscribe a :class:`HarnessEventListener` for ``agent_id``."
source: "harness_core/terminal_io/event_listener.py"
---

Create and subscribe a :class:`HarnessEventListener` for ``agent_id``.

## Signature
```python
subscribe_event_listener(agent_id: str, bus: Optional[EventBus]) -> EventListener
```

## References
- [Module: harness_core.terminal_io.event_listener](harness_core_terminal_io_event_listener) - Parent module
