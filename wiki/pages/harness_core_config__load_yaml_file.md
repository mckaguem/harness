---
name: "harness_core.config._load_yaml_file"
description: "Load a YAML file if it exists, returning an empty dict otherwise."
source: "harness_core/config.py"
---

Load a YAML file if it exists, returning an empty dict otherwise.

## Signature
```python
_load_yaml_file(path: Path) -> dict
```

## References
- [Module: harness_core.config](harness_core_config) - Parent module
